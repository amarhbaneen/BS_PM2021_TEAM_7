pipeline {
    agent none
    stages {

        stage('Test') {
            agent {
                docker {
                    image 'bm_team7/django_image'
                }
            }
            steps {
                sh 'python manage.py test'
            }
            post {
                always {
                    junit 'tests/results.xml'
                }
            }
        }
    }
}