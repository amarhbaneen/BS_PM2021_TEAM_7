pipeline {
    agent none
    stages {

        stage('Test') {
            agent {
                docker {
                    image 'django_image'
                }
            }
            steps {
                sh 'python manage.py test'
            }
            post {
                always {
                    junit 'test-reports/results.xml'
                }
            }
        }
    }
}